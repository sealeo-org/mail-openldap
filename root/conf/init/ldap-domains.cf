server_host = ldap
server_port = 389
search_base = dc=mail,%DC
query_filter = (&(description=virtualDomain)(dc=%s))
result_attribute = dc
bind = yes
bind_dn = %CN_ADMIN
bind_pw = %LDAP_PASSWORD
version = 3
